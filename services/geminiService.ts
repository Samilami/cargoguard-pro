// Dieser Service wurde deaktiviert, da keine KI-Funktionen mehr gewünscht sind.
// Die Datei bleibt als Platzhalter erhalten, um Import-Fehler in nicht aktualisierten Dateien zu vermeiden,
// falls diese noch existieren sollten.

export const analyzeDeliveryNote = async (imageDataUrl: string) => {
  return {};
};

export const analyzeDamageImage = async (imageDataUrl: string) => {
  return {};
};